# 2026-04-22 개발 로그 — 프로덕션 배포 파이프라인

## 배경
`v0.2.0`까지는 로컬 개발 전용 컴포즈(`runserver`)만 있었음. 실제 운영 배포를 위해 **GitHub Actions → GHCR → 시놀리지 NAS** 자동화 파이프라인이 필요.

배포 타겟으로 고려했던 옵션
- AWS EC2 맨땅 — gunicorn/systemd/nginx 직접 구성
- 맥을 서버로 활용 — Remote Login·절전 설정·공유기 포트포워딩 필요
- **시놀리지 NAS (최종 선택)** — DSM이 역방향 프록시·Let's Encrypt·DDNS 다 처리, 배포 인프라 작업 최소화

---

## 1. 프로덕션 런너 전환

**`Dockerfile.prod` 신설**
- 베이스: `python:3.11-slim`
- 빌드 시: `apt-get install build-essential libpq-dev` → pip install → 빌드 도구 purge
- `collectstatic --noinput`을 빌드 단계에서 실행 (런타임 I/O 절약)
- 비루트 사용자 `app` (UID 1000)로 실행 권한 이관
- CMD: `migrate --noinput && gunicorn --workers 3 --threads 4 --worker-class gthread --timeout 120`

**`requirements.txt`**
- `gunicorn>=22`, `whitenoise>=6.6` 추가

**`AI_Chat/settings.py` 환경변수화**
- `DJANGO_DEBUG` — 기본 `False` (운영 안전). 로컬은 `.env`에 `True` 명시
- `DJANGO_ALLOWED_HOSTS`, `DJANGO_CSRF_TRUSTED_ORIGINS` — 쉼표 구분 env 리스트 헬퍼
- `WhiteNoise` 미들웨어 + `CompressedManifestStaticFilesStorage` 등록
- `DEBUG=False` 시 `SECURE_PROXY_SSL_HEADER`, `SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE` 자동 활성화

**`.dockerignore` 신설**
- `.env*`, `.git/`, `resources/`, `staticfiles/`, `__pycache__/` 등 제외
- 없을 때: `.env`가 이미지에 박혀 GHCR로 유출될 위험 → 치명적
- 적용 후 이미지 `/app` 크기 ~6 MB

---

## 2. 시놀리지용 컴포즈

**`docker-compose.prod.yml`**
- `web`: `ghcr.io/sean-59/personal_ai_chat:${APP_IMAGE_TAG:-latest}` → 호스트 7008 : 컨테이너 8000
- `db`: `pgvector/pgvector:pg17` → 호스트 8007 : 컨테이너 5432 (DB 관리도구 접속용)
- 볼륨: `./resources` (업로드 파일), `./pgdata` (DB) — 시놀리지 `/volume1/ai_chat/` 하위에 영속화
- `restart: unless-stopped` 로 재부팅·크래시 복구
- `db` healthcheck(`pg_isready`) 통과 후 web 기동

**`.env.prod.example.txt`**
- 운영 전용 템플릿. `DJANGO_DEBUG=False`, 도메인·CSRF Origin 프리셋(`aicb.ipstein.myds.me`)
- `APP_IMAGE_TAG=latest` — 배포 시 Actions가 자동 치환

---

## 3. GitHub Actions 워크플로우

**`.github/workflows/deploy.yml`**
- 트리거: `push` 태그 `v*` + `workflow_dispatch`(수동)
- **build-and-push 잡**
  - `docker/setup-buildx-action` + `build-push-action`
  - `Dockerfile.prod` 빌드 → `ghcr.io/sean-59/personal_ai_chat:<태그>` 와 `:latest` 둘 다 push
  - GitHub Actions 캐시(`type=gha`)로 재빌드 가속
  - `GITHUB_TOKEN` + `packages:write` 권한으로 GHCR 인증
- **deploy 잡**
  - `SSH_PRIVATE_KEY` / `DEPLOY_HOST` / `DEPLOY_USER` / `DEPLOY_PORT` Secrets 사용
  - 시놀리지 SSH → `/volume1/ai_chat/.env`의 `APP_IMAGE_TAG` 값 `sed`로 치환
  - `docker compose pull && up -d && image prune -f`

---

## 4. 초기 셋업 요구사항 (1회)

### GitHub
- `Settings → Actions → General → Workflow permissions` = **Read and write** (GHCR push)
- Secrets 4개: `SSH_PRIVATE_KEY`, `DEPLOY_HOST=ipstein.myds.me`, `DEPLOY_USER=seonkyu.kim`, `DEPLOY_PORT=64`
- 첫 배포 후 **Packages** 탭에서 GHCR 가시성 **Public** 전환 권장 (시놀리지 pull 인증 생략 가능)

### 시놀리지
- `/volume1/ai_chat/` 생성
- `docker-compose.prod.yml` → `docker-compose.yml`로 이름 바꿔 배치
- `.env` 작성 — 로컬과 반드시 달라야 할 값:
  - `DJANGO_DEBUG=False`
  - `DJANGO_ALLOWED_HOSTS=aicb.ipstein.myds.me`
  - `DJANGO_CSRF_TRUSTED_ORIGINS=https://aicb.ipstein.myds.me`
  - 새 `DJANGO_SECRET_KEY` / `POSTGRES_PASSWORD` / `OPENAI_API_KEY`
- SSH 공개키(`ai_chat_deploy.pub`)를 `~/.ssh/authorized_keys`에 등록
- 홈·`.ssh` 권한: `755 ~` / `700 ~/.ssh` / `600 ~/.ssh/authorized_keys`
- DSM 역방향 프록시: `https://aicb.ipstein.myds.me` → `localhost:7008` — 이미 셋업됨

---

## 5. 배포 흐름

```
git tag v0.3.0 && git push origin v0.3.0
  ↓ (on: push tags v*)
GitHub Actions
  ├── build-and-push: Dockerfile.prod 빌드 → GHCR push
  └── deploy: SSH → APP_IMAGE_TAG 갱신 → compose pull/up
        ↓
시놀리지 컨테이너 재기동
  ├── db: pgdata 볼륨 유지
  └── web: migrate 자동 실행 (pgvector + pg_trgm 확장 포함)
        ↓
https://aicb.ipstein.myds.me 접속 가능
```

첫 배포 시 `migrate`가 `chat/0001_vector_extension`, `files/0003_trigram_search`에서 두 extension을 자동 설치하므로 수동 SQL 불필요.

---

## 테스트/검증 체크리스트

- [x] `Dockerfile.prod` 로컬 빌드 성공 (130 static files, image /app ≈ 6 MB)
- [x] `.dockerignore` 로 `.env`, `.git`, `resources` 제외 확인
- [x] 로컬 dev 서버 기존대로 동작 (HTTP 200, static 서빙)
- [x] 시놀리지 SSH 키 인증 (`echo ok`)
- [x] 시놀리지 `docker compose config` 문법 통과
- [ ] `v0.3.0` 태그 push → Actions 성공 → 시놀리지 웹 정상
- [ ] `https://aicb.ipstein.myds.me` 첫 로드 + 채팅 왕복 확인
